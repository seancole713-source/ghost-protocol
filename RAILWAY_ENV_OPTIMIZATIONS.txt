# Railway Environment Variable Optimizations
# Apply these in Railway Dashboard → Variables tab

# PROVIDER TIMEOUT OPTIMIZATION (reduce from default 6.0s to 1.0s)
PRICE_PROVIDER_TIMEOUT_S=1.0

# QUORUM SETTINGS (disable for single-provider speed)
PRICE_MIN_PROVIDERS=1
PRICE_REQUIRE_QUORUM=0

# FRESHNESS AND CACHING (balance real-time vs latency)
DATA_FRESHNESS_SEC=90
PRICE_CACHE_TTL=60
PRICE_TTL_S=30

# EXISTING OPTIMIZATIONS (keep these)
FOCUS_WOLF_ONLY=0
PRICE_STRICT_LIVE=1
STOCK_PRICE_SOURCE=polygon

# Apply via Railway CLI:
# railway variables set PRICE_PROVIDER_TIMEOUT_S=1.0
# railway variables set PRICE_MIN_PROVIDERS=1
# railway variables set PRICE_REQUIRE_QUORUM=0
# railway variables set DATA_FRESHNESS_SEC=90
# railway variables set PRICE_CACHE_TTL=60
# railway variables set PRICE_TTL_S=30
