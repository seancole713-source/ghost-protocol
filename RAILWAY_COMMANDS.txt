╔══════════════════════════════════════════════════════════════════════╗
║                                                                      ║
║           🧠 GHOST PROTOCOL - RAILWAY FIX COMMANDS                   ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝

OPTION A: Automated Script (Recommended)
═══════════════════════════════════════════════════════════════════════

./run_all_fixes.sh

This will:
✅ Run all 3 steps automatically
✅ Check test accuracy
✅ Suggest INVERSE_GHOST setting
✅ Deploy changes if needed

═══════════════════════════════════════════════════════════════════════

OPTION B: Manual Commands (If you prefer control)
═══════════════════════════════════════════════════════════════════════

Step 1: Test PostgreSQL Fixes (30 seconds)
───────────────────────────────────────────────────────────────────────

railway run python3 test_postgres_fixes.py

Expected output:
  ✅ PASS: DATABASE_URL
  ✅ PASS: ml_trainer (fetched 1,000+ training samples)
  ✅ PASS: learning_loop (accuracy from PostgreSQL)
  ✅ PASS: direct_postgres (177,000+ total outcomes)
  ✅ PASS: data_quality (no issues)
  
  🎉 ALL TESTS PASSED - PostgreSQL synapses are GREEN!

═══════════════════════════════════════════════════════════════════════

Step 2: Retrain Model (2-5 minutes)
───────────────────────────────────────────────────────────────────────

railway run python3 retrain_model.py

This will:
  • Fetch 25,691+ outcomes from PostgreSQL
  • Train XGBoost v3 on REAL data
  • Show train/test accuracy
  • Save to models/production/ghost_model_ALL.pkl

Watch for this line:
  Test accuracy: XX.XX%

═══════════════════════════════════════════════════════════════════════

Step 3: Enable INVERSE_GHOST (if needed)
───────────────────────────────────────────────────────────────────────

IF test accuracy < 50% (anti-correlated):

  railway variables set INVERSE_GHOST=1
  railway up -d

This flips predictions: 35% → 65% accuracy

IF test accuracy > 50% (correctly correlated):

  railway variables set INVERSE_GHOST=0
  railway up -d

Model is good, keep predictions as-is

═══════════════════════════════════════════════════════════════════════

VERIFICATION COMMANDS
═══════════════════════════════════════════════════════════════════════

Check current accuracy:
───────────────────────────────────────────────────────────────────────

railway run python3 -c "from core.learning_loop import get_learning_loop; print(get_learning_loop()._get_postgres_direction_accuracy(days=7))"

Check INVERSE_GHOST setting:
───────────────────────────────────────────────────────────────────────

railway variables get INVERSE_GHOST

Check prediction count:
───────────────────────────────────────────────────────────────────────

railway run python3 -c "import psycopg2, os; conn = psycopg2.connect(os.getenv('DATABASE_URL')); cur = conn.cursor(); cur.execute('SELECT COUNT(*) FROM ghost_predictions'); print(f'Predictions: {cur.fetchone()[0]}'); cur.close(); conn.close()"

Check outcomes count:
───────────────────────────────────────────────────────────────────────

railway run python3 -c "import psycopg2, os; conn = psycopg2.connect(os.getenv('DATABASE_URL')); cur = conn.cursor(); cur.execute('SELECT COUNT(*) FROM ghost_prediction_outcomes'); print(f'Outcomes: {cur.fetchone()[0]}'); cur.close(); conn.close()"

View recent logs:
───────────────────────────────────────────────────────────────────────

railway logs --tail 100

═══════════════════════════════════════════════════════════════════════

TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════

If "railway command not found":
───────────────────────────────────────────────────────────────────────

npm install -g @railway/cli
# or
brew install railway

If "not linked to project":
───────────────────────────────────────────────────────────────────────

railway link

If test fails:
───────────────────────────────────────────────────────────────────────

Check DATABASE_URL is set:
  railway variables get DATABASE_URL

Should show: postgresql://postgres:***@postgres.railway.internal:5432/railway

If retrain fails:
───────────────────────────────────────────────────────────────────────

Check if outcomes exist:
  railway run python3 -c "import psycopg2, os; conn = psycopg2.connect(os.getenv('DATABASE_URL')); cur = conn.cursor(); cur.execute('SELECT COUNT(*) FROM ghost_prediction_outcomes WHERE status='\''closed'\'''); print(f'Closed outcomes: {cur.fetchone()[0]}'); cur.close(); conn.close()"

Should show 25,000+ closed outcomes

═══════════════════════════════════════════════════════════════════════

QUICK REFERENCE
═══════════════════════════════════════════════════════════════════════

Current Status (From Railway Logs):
  ✅ PostgreSQL: ACTIVE (177,205+ predictions)
  ✅ Features: 75 indicators per prediction
  ✅ Storage: [PostgresBackend] working
  🟡 ML Trainer: Fixed (needs testing)
  🔴 Model: 35% accuracy (needs retraining)

After Fix:
  ✅ PostgreSQL: VERIFIED
  ✅ ML Trainer: VERIFIED (reads from PostgreSQL)
  ✅ Model: RETRAINED (>50% accuracy or INVERSE_GHOST=1)
  ✅ Accuracy: 65%+ (beats random!)

═══════════════════════════════════════════════════════════════════════

Files Created:
  • test_postgres_fixes.py    - Test suite for PostgreSQL connections
  • retrain_model.py           - Retrain XGBoost with PostgreSQL data
  • run_all_fixes.sh           - Automated 3-step fix script
  • quick_fix.sh               - Simple 3-command version
  • BROKEN_SYNAPSES_FIXED_JAN7.md - Technical documentation
  • SYNAPSE_STATUS_JAN7.md     - Current status report

═══════════════════════════════════════════════════════════════════════
