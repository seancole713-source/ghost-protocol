✨ GHOST RAILWAY CLI - QUICK START
====================================

┌─────────────────────────────────────────────────┐
│  🚀 ONE-COMMAND DEPLOY                          │
└─────────────────────────────────────────────────┘

   ./deploy_ghost.sh

   → Installs Railway CLI
   → Authenticates
   → Sets environment variables  
   → Deploys Ghost
   → Tests health
   → Shows URL

   ⏱️  Takes: ~5 minutes
   ✅ Works: First time & always


┌─────────────────────────────────────────────────┐
│  🔄 QUICK UPDATES                               │
└─────────────────────────────────────────────────┘

   ./redeploy.sh "Your message"

   → Git commit
   → Git push
   → Railway deploy

   ⏱️  Takes: ~3 minutes
   ✅ Use: After code changes


┌─────────────────────────────────────────────────┐
│  🎛️  MANAGEMENT COMMANDS                        │
└─────────────────────────────────────────────────┘

   ./railway_manage.sh logs        # Watch logs
   ./railway_manage.sh health      # Test endpoint
   ./railway_manage.sh url         # Get URL
   ./railway_manage.sh status      # Check status
   ./railway_manage.sh restart     # Restart service
   ./railway_manage.sh restore     # Restore position


┌─────────────────────────────────────────────────┐
│  📊 TYPICAL WORKFLOW                            │
└─────────────────────────────────────────────────┘

   # 1️⃣  First Time
   ./deploy_ghost.sh
   
   # 2️⃣  Make code changes
   vim wolf_app.py
   
   # 3️⃣  Redeploy
   ./redeploy.sh "Fixed price provider"
   
   # 4️⃣  Check health
   ./railway_manage.sh health
   
   # 5️⃣  Watch logs (optional)
   ./railway_manage.sh logs


┌─────────────────────────────────────────────────┐
│  🆘 TROUBLESHOOTING                             │
└─────────────────────────────────────────────────┘

   Problem: Deployment fails
   Solution: ./railway_manage.sh logs

   Problem: Health check fails
   Solution: ./railway_manage.sh restart

   Problem: Can't get URL
   Solution: ./railway_manage.sh url

   Problem: Position data lost
   Solution: ./railway_manage.sh restore


┌─────────────────────────────────────────────────┐
│  📁 FILES CREATED                               │
└─────────────────────────────────────────────────┘

   deploy_ghost.sh        → Full deployment automation
   redeploy.sh            → Quick updates
   railway_manage.sh      → Management CLI
   RAILWAY_README.md      → Detailed guide
   
   + Procfile             → Railway start command
   + nixpacks.toml        → Dependency installation
   + railway.toml         → Health checks & cron


┌─────────────────────────────────────────────────┐
│  ✅ SUCCESS INDICATORS                          │
└─────────────────────────────────────────────────┘

   ✓ ./deploy_ghost.sh completes
   ✓ Health check returns {"ok": true}
   ✓ URL opens Ghost UI in browser
   ✓ /api/cockpit returns JSON
   ✓ Ghost runs 24/7 on Railway


┌─────────────────────────────────────────────────┐
│  🎯 START NOW                                   │
└─────────────────────────────────────────────────┘

   Just run:
   
   ./deploy_ghost.sh
   
   That's it! ✨


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Made with ❤️  for Ghost Trading System
